<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
    <script src="javascripts/buttonredirects.js" defer></script>
  </head>
  <body>
    <header class="banner">
      <% include partials/header %>
    </header>
    <main>
        <h2>Report for name</h2>
        <table id="bookingsTable">
        <thead>
            <tr>
            <th>Name</th>
            <th>ID</th>
            <th>Booking Date</th>
            <th>Creation Date</th>
            <th>Card No</th>
            <th>Expiry Date</th>
            <th>Security Code</th>
            </tr>
        </thead>
        <tbody>
            <% bookings.forEach((b) => {%>
            <tr>
                <td><%= b.name %></td>
                <td><%= b.id %></td>
                <td><%= b.bookingDate.toDateString() %></td>
                <td><%= b.creationDate.toDateString() %></td>
                <td><%= b.cardNo %></td>
                <td><%= b.expiryMonth %>/<%= b.expiryYear %></td>
                <td><%= b.securityCode %></td>
            </tr>
            <% }); %>
        </tbody>
        </table>
    </main>
    <footer class="banner">
      <% include partials/footer %>
    </footer>
  </body>
</html>
